{
  "name": "chirp",
  "version": "2.0.0",
  "description": "Twitter clone with two-tier LLM moderation via Ollama Cloud - deployed on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute chirp-db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute chirp-db --remote --file=./migrations/0001_init.sql"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:42875/git/Moebe1/Ai-sm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
